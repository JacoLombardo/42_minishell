valgrind --suppressions=readline.supp --leak-check=full --show-leak-kinds=all ./minishell

// when a variable exists both in env and in temps, bash checks temps first. ours for now checks env first




// echo with no args segfaults -- still happens 4nov
// unset 
🫠 :~/..  export FRIEND=calamardo
🫠 :~/..  echo $FRIEND
calamardo
🫠 :~/..  unset FRIEND
🫠 :~/..  echo $FRIEND
calamardo
🫠 :~/..  unset $FRIEND
free(): invalid pointer
Aborted (core dumped)

// echo doesnt print spaces between its args🫠 :~/minish  unset 
🫠 :~/minish  echo hola ahsj djdj  j j j jj jj jj j jjjjj j j j 
holaahsjdjdjjjjjjjjjjjjjjjjjjj

// what happens -what is supposed to happen- when not completing inside a quote like
echo "hola

real bash gives a prompt to complete it. what do we have to do?
also this + heredoc

// 
🫠 :~/minish  unset 
🫠 :~/minish  export FRIEND=calamardo
🫠 :~/minish  echo $FRIEND

🫠 :~/minish  

// when a command doesnt exist minishell exits 
// after heredoc it also exits

todo HEREDOC: when <<"EOF" exit condition should be EOF not "EOF"

done:

Variable expansion is the default behavior inside of here-docs. You disable that behavior by quoting the label (with single or double quotes).

